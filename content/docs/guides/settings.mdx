---
title: Clinic settings & team
description: Your profile and preferences, the care team, and running more than one clinic.
icon: Settings
---

The **Settings** page is where you manage your own profile and preferences, and where
owners and admins manage the clinic itself — most importantly, the **care team**.

## Your profile & preferences

The **Profile** tab holds your display name, contact details, and notification
switches (patient notifications, account notifications). These are real, persisted
settings:

- Changes are tracked as you make them — a **Save changes** bar appears at the bottom
  of the page (and stays visible while you scroll) whenever something is unsaved.
- Saved preferences are stored **per user on the server**
  ([`/api/settings`](/docs/api/settings)), so they follow you across devices.
- The **Features** section (patient-owned storage, required signing) previews
  [roadmap](/docs/roadmap) functionality and isn't active yet.

### Deleting your account

At the bottom of the Profile tab, **Delete account** opens a confirmation dialog that
asks for your **password**. Confirming permanently deletes the account, signs you out
everywhere, and cannot be undone.

## Managing your team

### Inviting staff

1. Open **Settings** and find the care team section.
2. Enter the colleague's **email address** and choose their
   [role](/docs/admin/roles-permissions).
3. Send the invitation. They receive a link; opening it takes them to the
   **accept invite** page, where they create an account (or sign in) and join your
   clinic with the role you chose.

### Creating staff accounts directly

Admins can also create accounts for staff without the email round-trip: set a
**username** and password, and hand the credentials over. Staff created this way sign
in with their **username** instead of an email address — handy for front-desk accounts.

### Managing members

From the same page you can see every member and their role. **Click a team member** to
open their detail dialog: it shows exactly what their role lets them do (per-resource
permission chips for patients, appointments, prescriptions, and tasks), and lets
admins **change the role** or **remove the member**. Only owners and admins can manage
the team; the [permission matrix](/docs/admin/roles-permissions) has the details.

## Records, Signing & Developers tabs

Owners and admins also see three more tabs:

- **Records** — where patient records come from (the clinic database today,
  patient-owned storage later), import/export, and retention.
- **Signing** — your signing key and identity, and how the
  [patient-approval flow](/docs/concepts#patient-owned-records-the-vision) will work.
  Illustrative until signing ships.
- **Developers** — the API base URL and a preview of personal access tokens
  (see the [API reference](/docs/api)).

## Multiple clinics

A single temetro account can belong to many clinics. Use the **clinic switcher** in the
sidebar to move between them — patients, schedules, messages, and settings are all per
clinic, completely separated from one another.

## Account basics

- **Sign in** with email or username, plus your password.
- **Forgot password?** Use the reset link on the sign-in page — you'll get an email
  with instructions.
- Email verification is currently **optional** — you can sign in without verifying —
  but verification emails are already sent and enforcement is
  [planned](/docs/roadmap#email-verification-enforcement).
